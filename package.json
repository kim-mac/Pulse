{
  "name": "pulseboard",
  "version": "1.0.0",
  "private": true,
  "description": "PulseBoard multi-provider relay and single-file frontend",
  "scripts": {
    "start": "node server.js",
    "test:structure": "node pulseboard-structure.test.js && node pulseboard-relay.test.js && node pulseboard-csv.test.js"
  }
}
